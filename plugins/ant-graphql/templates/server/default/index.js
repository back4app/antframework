/**
 * Exports the {@link Server} class that can be used to initialize and start the
 * Ant Framework's default GraphQL API server.
 * @module antframework/lib/plugins/GraphQL/templates/server/default
 */

const Server = require('./lib/Server');

module.exports = Server;
